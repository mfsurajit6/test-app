import React from 'react'
import { Typography } from '@material-ui/core'

const Home = () => {
    return (
        <div>
            <Typography variant="h3" style={{textAlign:'center', marginTop: '20px'}}>Welcome to Home Page</Typography>
        </div>
    )
}

export default Home
